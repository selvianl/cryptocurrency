Run these commands respectively at the location where the manage.py file is located. <br>

1. docker build -t btx . <br>
2. docker-compose up

<br>

A test api exposed. It reachable at `localhost:8000`.
